Singly linked lists have the advantage of being 4*n bytes smaller than doubly linked lists.

Doubly linked lists are faster than singly linked lists because they can be traversed from both directions.
